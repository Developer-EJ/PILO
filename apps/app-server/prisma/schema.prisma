// Physical Prisma anchor for future app-server persistence work.
// The current DB contract source of truth is docs/db/pilo_erd_schema.sql.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
